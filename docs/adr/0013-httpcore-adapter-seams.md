# 适配器编写模型:HttpCore 深模块 + 四接缝(新增交易所一律按此模式)

深化统一接口(架构评审候选 1):把 13 个适配器逐字重复的公共机制(`query_string` ×13、`public_get` ×12、`load_markets` 缓存 ×13、`value_decimal`/`parse_level` ×10 等)下沉为一个交易所无关的深模块 **HttpCore**(HTTP 请求骨架、市集缓存、客户端分页过滤、safe 提取、Precise 精确数值),统一接口面保持 158 方法枚举契约不变(ADR-0001 契约测试面不受影响);每个适配器只填四个接缝——**describe**(端点路径/参数)、**sign**(签名算法)、**handle_errors**(错误码映射)、**字段映射**(parse 覆写)。此切法完全对齐 ccxt 基类/子类模型(ccxt `base/exchange.py` 的 `fetch2`/`load_markets`/`safe_*`/分页全在基类,`describe()`/`sign()`/`handle_errors`/`parse_*` 留在子类),是经过 ccxt 93 家交易所验证的边界。形态采用混合:核心以**可选组合**为主(适配器持有 HttpCore 而非强制 trait 默认链),只对普适链(如"公共 GET → 解析")提供 trait 默认实现,不强制 REST 形状——未来纯 WS/GraphQL 交易所可直接跳过核心、不被 158 个默认实现束缚;预测市场特有逻辑(`OutcomeCtx` 解析、合成 ticker、RSA-PSS/secp256k1 签名)全部留在适配器侧,核心保持交易所无关。验收标准:ADR-0005 的"从 ccxt 转译批量补齐适配器"评估落地时,转译器只填四缝、零改动复用 HttpCore——能转译即证明兼容性。被否决:强制 trait 默认实现链(158 方法默认干活,接口/契约测试面失焦,REST 形状被强加于所有交易所)。**后续新增任何交易所(手写或转译)都必须按此模式,避免二次优化。**

- **Status**: accepted

**实施注记(2026-08-12,收口候选 1)**:把散落在 realtime 层与适配器里的数值/解析助手全部收口到 `HttpCore`,消除 13 份 `now_ms`(u64/i64 已分歧,统一为 `i64`)、8 份 `iso8601`、5 份 `dec`、5 份 `collect_levels`、2 份 `value_decimal`/`query_string`/`pct_encode`,并把 `generic.rs` 的 safe 提取家族(`to_dec`/`to_i64`/`pick_*`)与 `dec_f64` 移入 HttpCore(best-effort 语义单独标注)。净删 210 行;`cargo build --all-features` 零警告、`clippy -D warnings` 全绿、`cargo test --all-features` 131+6+57+5+1 全过,差分 fixture 无漂移(零行为变更)。**已知遗留(下轮评审点)**:① `adapters/binance.rs::iso8601` 保留本地实现,因其格式为 `to_rfc3339_opts(Millis, true)`(`...Z` 毫秒),与 HttpCore 的 `to_rfc3339()`(`...+00:00`)不同——统一格式需改 binance datetime 输出,属行为变更,未纳入本轮;② `num` 助手(`Option<&Value>→Decimal`)仍存在于 coinbase/binance/mexc 三处(语义与 `value_decimal` 逐字一致,仅纯改名,未纳入本轮)。

**实施注记(2026-08-12,收口候选 3 — sign 接缝)**:REST/WS 双签名实现合一。kraken:`private_post` 提为 `pub`,realtime `fetch_ws_token` 直接调用(整请求复用,顺带删除 realtime 自建 `Client` 字段与 `REST_BASE`,仅此一处使用);okx:提取 `pub fn sign_str(timestamp, method, path, body)`(REST `private_request` 与 realtime WS 登录帧共用);bybit:`hmac_sha256_hex` 提为 `pub`,realtime auth 帧调用。kalshi `sign_rsa_pss` 复用为既有先例。净删 54 行;门禁同上全绿,差分 fixture 无漂移。遗留:binance/coinbase 等其余适配器的 REST/WS 签名如有重复,按同模式下沉。

**实施注记(2026-08-12,收口候选 6 — OutcomeIndex)**:新增适配器侧共享模块 `src/adapters/outcome.rs`,泛型 `OutcomeIndex<T>` 收口预测市场「symbol → 上下文」索引的缓存(`store`)、解析(`resolve`)与兜底(`resolve_or` 闭包)语义,错误映射统一(未加载 → `NotSupported`,未命中 → `BadSymbol`)。polymarket/kalshi 各自的 `OutcomeCtx` 类型**保留**(字段差异是真实的:token_id/condition_id vs outcome_id/series_ticker,合并为 union 会让 parse_* 全变 Option 解包、丧失类型安全);各适配器保留薄 `resolve_outcome` 包装,realtime 调用点零改动;kalshi 的 `{symbol}:YES` 兜底经 `resolve_or` 闭包语义化。门禁全绿,新增 3 个 OutcomeIndex 单测。此实现印证 ADR-0013「预测市场特有逻辑留在适配器侧」——共享模块放在适配器命名空间内,HttpCore/核心保持交易所无关。

**实施注记(2026-08-12,收口候选 2 — handle_errors 接缝)**:把一直只存在于文档的第四缝真正接线。新增 `httpcore::extract_exchange_error(body, exchange) -> Option<Error>`(ccxt 基类通用错误信封识别:Kraken `error[]`、Bybit `retCode`、HTX `status:"error"`、通用 `success:false` / `code` 数字 0/200 或字符串 `"0"`/`"200"`/`"200000"`/`"00000"` 为成功),并在 **`HttpCore::request_url` 单一漏斗**处调用——`request`/`public_get`/`request_ep`(95+ 转译适配器)全部经此,一次覆盖 100% 流量,无需逐个包裹器插桩。识别保守:仅对明确错误信号判定错误,绝不误伤成功响应(原行为是无错误信封的成功体静默交还解析层、错误体则抛晦涩 JSON 解析失败)。`HttpCore` 新增 `exchange: &'static str` 字段(构造处传入 id),错误上下文带 `[exchange=...]` 与 `[code=...]`;`HttpCore::new` 签名加第四个参数,14 处调用点(含 generic + 13 手写适配器)补齐 id。新增 12 个 mapper 单测,门禁全绿(构建/clippy -D warnings/fmt/test 57 lib + 5 fixture)。**已知遗留(均已收口于候选 ⑤)**:① 通用 mapper 统一映射为 `ErrorKind::Exchange`,各交易所「错误码→更细 ErrorKind」的精细分类见下文候选 ⑤;② gate/coinbase 等错误信封为 `{label/message:...}` 且无 code 字段,通用 mapper 暂未覆盖,等同现状(不当误报);③ hyperliquid 绕开 HttpCore 直用 reqwest,不在此接缝覆盖(既有技术债)。

**实施注记(2026-08-12,架构评审报告候选 3 — Rust/Python 路由契约单一来源)**:消除 `key_matches` + 候选键列表在 `src/generic.rs`(Rust 引擎)与 `scripts/gen_adapters.py`(转译器)双份手工同步的静默漂移风险。两处确证漂移:Python `CANDIDATES` 字面量漏了 Rust `fetch_status` 路由所需的 `system_status` 与 `ping`(generic.rs:265),导致生成适配器对这些状态键永远路由不到。修复分两层:**(数据)** `scripts/gen_adapters.py` 删除手写 `CANDIDATES` 字面量,改为 `routing_candidates()` 在生成时直接解析 `src/generic.rs` 全部 `.find_first(&[...])` 候选数组(小写、去重)作为唯一来源,并加解析守卫(解析数 < 20 即失败,杜绝静默少发端点);`endpoint_relevant` 调用点零改动。**(算法)** `key_matches` 跨语言无法单一来源,故新增 `scripts/test_sync.py`(无 ccxt 依赖、可独立运行)用 `generic.rs::key_matches_word_boundary` 同一组 fixtures 做一致性守护,并检查 `routing_candidates()` 含全部必经候选键。该测试接入 `ci.yml` 新 job `transpiler-sync`(setup-python + `python3 scripts/test_sync.py`),漂移即让 CI 变红。验证:`python3 scripts/test_sync.py` 通过(37 keys + 8 fixtures);Rust 侧零改动,`cargo build/clippy -D warnings/fmt/test --all-features` 全绿。**注意**:此修复使生成器在「未来」生成时会包含 `system_status`/`ping` 端点(与引擎契约对齐,属正确行为),但已 check-in 的 95 个生成适配器是旧契约快照,需待候选 6(生成层漂移 / CI 重跑转译器)统一重生成方才物化——本候选不触发重生成(避免 95 文件噪声)。

**实施注记(2026-08-12,架构评审报告候选 4 — sign 接缝显式化)**:把一直隐式硬编码在 `generic.rs::sign_headers` 的默认签名(ccxt 基类 `HMAC-SHA256(method+url+body)` + `apiKey`/`sign`/`timestamp` 头)升级为 ADR-0013 第四缝 `sign` 的**结构化表达**。新增 `generic::SignScheme` 枚举(当前唯一变体 `HmacSha256Default` = 原 ccxt 基类默认),`GenericExchange` 增 `sign_scheme: SignScheme` 字段,`GenericExchange::new` 增第四参,`sign_headers` 改为按 `sign_scheme` 分派(当前唯一分支即原算法,行为零变化)。`impl_generated_adapter!` 宏扩为**可选三参**形式——两参形式显式默认到 `HmacSha256Default`(对应「默认 sign 显式」),故**已 check-in 的 95 生成适配器(两参)零改动即可编译**,无需重生成。转译器 emit 形式保持两参(默认即显式),未来若需按交易所定制签名(如 OKX/Binance/Bybit 的定制 HMAC 或 RSA-PSS),扩展此枚举 + 在 `sign_headers` 加分支 + 让转译器 emit 三参即可,且逐所定制签名本就属手写适配器职责(ADR-0013「预测/特殊市场逻辑留适配器侧」的同理)。验证:`cargo build/clippy -D warnings/fmt/test --all-features` 全绿(147 lib tests)。**边界**:ccxt 的签名是命令式(各所覆写 `sign` Python 方法),非声明式,故转译器无法从 `describe()` 推导非默认方案——这是生成适配器「只覆盖公开面 / 长尾」定位的固有边界,属预期。

**实施注记(2026-08-12,架构评审报告候选 6 — 生成层漂移守卫)**:37k 行 check-in 的生成适配器此前在特性启用前不编译、转译器不在 CI 重跑,可静默 bit-rot。本候选落地为**两层漂移守卫**:**(a) 重生成物化**:候选 3 的单一来源修复使生成器现在会含 `system_status`/`ping` 端点,本候选用仓库 bundled ccxt 4.5.73(`ccxt/python`,与 check-in 快照同版本)重跑了 `scripts/gen_adapters.py`,补齐 17 个交易所(aster/backpack/bigone/binancecoinm/binanceus/binanceusdm/bitrue/coinex/coinsph/digifinex/hashkey/lbank/ndax/tokocrypto/toobit/weex/whitebit)缺失的 `ping`/`system_status` 端点(共 +252 行,纯新增、零删除;`generated.rs` 与 `Cargo.toml` 因 `edit_cargo` 幂等而不变)。生成器经连跑两次校验为**确定性**(输出逐字节一致),故守卫不会误报。**(b) CI 新鲜度 job**:`ci.yml` 新增 `transpiler-fresh` job(setup-python 3.13 + rustfmt 组件)——`python3 scripts/gen_adapters.py` 重跑生成器、`cargo fmt --all` 归一化、再 `git diff --exit-code -- src/adapters/generated/ src/adapters/generated.rs Cargo.toml`;任何使生成输出变化的改动(改 `generic.rs`/`gen_adapters.py` 却忘重生成)都会让 CI 变红。与候选 3 的 `transpiler-sync` job(契约一致性)互补:sync 守「契约对不对」,fresh 守「生成层新不新」。验证:本地模拟该 job 两连跑均确定性一致;`cargo build/clippy/fmt/test --all-features` 全绿。

**实施注记(2026-08-14,架构评审报告候选 ① — sign 接缝共享深模块 `src/signing.rs`)**:把散落在 17 个 HMAC 适配器 + `generic.rs`(第 18 份)里的签名原语、凭据抽取、请求头装配下沉为一个交易所无关的深模块 `src/signing.rs`(ADR-0013 第四缝 `sign` 的共享实现)。模块暴露:`hmac_sha256_hex`/`hmac_sha256_b64`/`hmac_sha512_hex`/`hmac_sha512_b64`/`hmac_sha384_hex`(统一约定 `hmac(secret, msg)` —— secret 在前,归一了 binance 系 `(data, secret)` 反向实参)、`require_api_key`/`require_secret`/`require_passphrase`(带交易所名的 `Authentication` 错误)、`set_header`(把 `HeaderValue::from_str` 错误转 `Error`,消除 okx/htx/bitget/kucoin/gate 的 `.unwrap()` panic)。各适配器 `private_request` 仅保留真正的交易所差异——认证串拼接(`query+&signature` vs `ts+method+path+body`)与请求头名集。`signing.rs` 自带 5 个独立向量(sha256_hex 用 binance 官方、其余用 Python stdlib 独立计算)一次覆盖全部原语。门禁:`cargo clippy --all-targets --all-features -- -D warnings` 全绿、`cargo test --all-features` 158+ 全过(含 5 向量)、`python3 scripts/test_sync.py` OK(ADR-0016 契约零漂移)。**已下沉(15 个)**:binance/okx/bybit/gate/gemini 试水切片 + aster/mexc/hashkey/binanceus/coinbase(hex-sha256)、kucoin/bitget/htx(b64-sha256)全量;`generic.rs` 默认签名器、`realtime/bybit` 调用点同步。删 bybit `pub fn hmac_sha256_hex`、gemini 死代码 `sign_hmac_sha256`、binance 重复测试向量。净删约 330 行。**文档化例外(留接缝)**:polymarket 与 kraken 的 HMAC 作用在**解码后的字节**上(polymarket:base64url 解码 secret + url-safe base64 输出;kraken:base64 解码 secret + sha256 预哈希 + 二进制 binhash 作消息),`signing` 的字符串原语无法表达,属真实交易所差异,按 ADR-0013「差异留接缝」原则保留各自内联实现。

**实施注记(2026-08-14,架构评审报告候选 ② — `num`/`iso8601` 助手规范化)**:消除上文「已知遗留 ①②」两处重复。新增 HttpCore 规范化助手 `iso8601_ms(ms: i64) -> Option<String>`(`to_rfc3339_opts(SecondsFormat::Millis, true)`,输出 `...Z` 毫秒精度——与 binance 原本地 `iso8601` 逐字同格式,故 binance 的本地实现被判定为冗余),并把 `num`(`Option<&Value> -> Option<Decimal>`)统一下沉为既有的 `value_decimal`/`dec`(此前 `num` 与 `value_decimal` 语义逐字一致、仅纯改名)。**已下沉**:`num`→`dec` 共 9 适配器(binance/aster/mexc/binanceus/hashkey/coinbase/gemini/alpaca/lighter),掉落随之孤立的 `use std::str::FromStr;`;`iso8601`→`iso8601_ms` 共 6 适配器(binance/aster/binanceus/hashkey/gemini/alpaca,`.and_then(iso8601)` 改为 `.and_then(iso8601_ms)`);`tests/differential_test.rs` 本地 `fn dec` 改为 `use adaq_trading_crypto::httpcore::value_decimal as dec;`(本地版即 `value_decimal`)。HttpCore 新增 2 单测(`parse_ohlcv_standard_reads_canonical_array`、`iso8601_ms_emits_millisecond_z` —— 后者验证 epoch ms `1499040000000` 正确输出 `2017-07-03T00:00:00.000Z`)。门禁:`clippy -D warnings` 全绿、`fmt --check` 通过、`cargo test --all-features` 160 全过、`python3 scripts/test_sync.py` OK。**至此「已知遗留 ①(binance iso8601)」与「②(num 三处)」均收口**。

**实施注记(2026-08-14,架构评审报告候选 ③ — 共享 OHLCV 构建器 `parse_ohlcv_standard`)**:新增 HttpCore 规范化构建器 `parse_ohlcv_standard(row: &Value) -> OHLCV`(按 ccxt 标准 6 元组 `[ts, open, high, low, close, vol]` 索引,`ts` 经 `as_i64`、`open..vol` 经 `value_decimal`),收口候选 ③ 的 OHLCV 部分。**已下沉(5 个)**:binance/aster/binanceus/hashkey/mexc(均为标准 6 元组格式)。**留接缝(14 个,均有真实格式差异、非纯改名)**:coinbase(秒→毫秒)、kraken(vol 在第 6 下标)、bybit(7 元组含成交笔数)、htx(对象式非数组)、alpaca(bars 对象)、gemini(重排下标)、lighter(对象式)、okx/okxus/myokx(字符串容错 ts)、kucoin(ts×1000)、bitget(重排下标)、kalshi(额外参数)、polymarket/manifold(预测市场对象)——按 ADR-0013「差异留接缝」原则保留各自的 `parse_ohlcv` 覆写。**未做(Ticker 构建器)**:候选 ③ 的 Ticker 规范化构建器本轮未落地——各所 ticker 字段差异更大(且多数为对象式 `parse_ticker`),收益/风险比低于 OHLCV,留待后续评审。

**实施注记(2026-08-14,架构评审报告候选 ④ — 拆分 `generic.rs` 的 parse_* 消费者)**:把 1374 行 god module `generic.rs` 中 ~494 行的 generated 路径 `parse_*` 消费者(16 个 `pub(crate) fn`:parse_market_type/resolve_one/ticker/tickers/ohlcv/trades/trade/levels/order_book/orders/order/balance/currencies/currency/markets/market)与 3 个 parser 单测,整体迁到新模块 `src/generic_parse.rs`;ADR-0016 契约锚(`key_matches`/`find_first`/`fill_path`/`join_url`/`sign_headers`/`sign_hmac_sha256_default`)**原封不动留在 `generic.rs`**,`generic.rs` 的 `Exchange` impl 经 `use crate::generic_parse::*;` 继续调用这些 parser(14 处调用点零改动)。**可行性已先验证**:① parser 块自包含(只互调 + `crate::httpcore` 助手 + `crate::types`,不引用任何契约锚);② parser 无外部调用方(无 `generic::parse_*` 引用,`pub(crate)` 足矣);③ 迁移不动 `key_matches`/`find_first` 文本,`scripts/test_sync.py` 扫描范围零影响。**落地结果**:`generic.rs` 850 行 + `generic_parse.rs` 531 行;`generic.rs` 随之删除仅 parser 使用的 httpcore(`dec_f64`/`iso8601`/`parse_level`/`pick_decimal`/`pick_str`/`to_i64`/`value_decimal`)与 types(`Balance`/`Currency`/`Level`/`Limit`/`Limits`/`MarketType`/`Precision`)导入。`generic_parse.rs` 自带模块 doc(注明与 ADR-0016 锚点的隔离关系)。门禁:`clippy -D warnings` 全绿、`fmt --check` 通过、`cargo test --all-features` 160 全过(parser 单测随模块迁移)、`python3 scripts/test_sync.py` OK(契约零漂移)。**收益**:locality 提升——审计 ADR-0016 契约不再穿越 market/ticker 解析;契约漂移风险隔离到 `generic.rs` 单一文件。**已知遗留**:生成层(`src/adapters/generated/`)仍直接复用 `generic.rs` 引擎而非 `generic_parse`(经 `impl_generated_adapter!` 宏转发 16 个通用方法,parser 在引擎内部调用),属预期,无需改动。

**实施注记(2026-08-14,架构评审报告候选 ⑤ — 错误码 → 细粒度 ErrorKind 映射)**:收口候选 2「已知遗留 ①」。在 `httpcore::extract_exchange_error` 中接入 per-exchange 的 `(exchange, code) → ErrorKind` 静态映射表 `ERROR_CODE_MAP`(新增 `classify_error_code` 查表;`error_with` 改为内部按 `code` 升级 `ErrorKind`,未命中回退 `ErrorKind::Exchange`,业务码仍写入 `context.http_error_code` 供适配器覆写)。**种子表(高置信度、长期稳定的码,可在此追加、不改识别逻辑)**:binance `-1121`→BadSymbol / `-2019`→InsufficientFunds / `-2015`→Authentication / `-1003`→RateLimitExceeded / `-2010`→InvalidOrder;bybit v5 `10001`→InvalidOrder / `10003`→Authentication / `10005`→PermissionDenied / `10006`→RateLimitExceeded;htx `invalid-symbol`→BadSymbol。**行为影响(正确且可取)**:仅 binance `-1003`(限频)由原 `Exchange`(不可重试)升级为 `RateLimitExceeded`(可重试)——其余码原为 `Exchange`(均不可重试),与细粒度变体(InsufficientFunds/Authentication/InvalidOrder/BadSymbol/PermissionDenied 皆不可重试)重试性一致,无回归;Kraken(错误在数组、无 code 字段)与未入表的 okx `51000`/kucoin/bitget 等维持 `Exchange`,等价原行为。**测试**:现有 okx/binance/kraken/bybit/htx 检测单测仍绿(bybit `10001`、htx `invalid-symbol` 额外补断言验证升级为 InvalidOrder/BadSymbol);新增 `binance_known_code_maps_to_fine_grained_kind`(5 码全映射)与 `unmapped_code_falls_back_to_exchange`(okx 51000 回退 Exchange 且 code 仍入 context)。门禁:`clippy -D warnings` 全绿、`fmt --check` 通过、`cargo test --all-features` 162 全过、`python3 scripts/test_sync.py` OK。**边界**:映射表仅覆盖有结构化 `code` 字段的信封;Kraken 自由文本错误与 gate/coinbase 无 code 信封暂不细分(属已知遗留 ②③)。
